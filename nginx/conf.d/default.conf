server {
    listen 80;
    server_name localhost;

    # 通知服务
    location /api/notify/ {
        proxy_pass http://notification-center:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 投资分析工具
    location /investment/ {
        proxy_pass http://investment-analyzer:8501/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # 支持WebSocket
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # API文档
    location /docs {
        proxy_pass http://investment-analyzer:8501/docs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 默认页面
    location / {
        return 200 '<!DOCTYPE html>
<html>
<head>
    <title>工具集</title>
    <meta charset="utf-8">
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .service { margin: 20px 0; padding: 20px; border: 1px solid #ddd; border-radius: 5px; }
        .service h2 { color: #333; }
        .service a { color: #0066cc; text-decoration: none; }
        .service a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <h1>🛠️ 工具集服务</h1>

    <div class="service">
        <h2>📧 通知服务</h2>
        <p>发送邮件和系统通知</p>
        <a href="/api/notify/">访问通知API</a> |
        <a href="/api/notify/docs">API文档</a>
    </div>

    <div class="service">
        <h2>📈 投资分析工具</h2>
        <p>股票投资数据分析</p>
        <a href="/investment/">访问投资分析</a>
    </div>

    <div class="service">
        <h2>🚗 北京车牌许可</h2>
        <p>北京车牌申请自动化工具</p>
        <span>（后台运行）</span>
    </div>

    <p style="margin-top: 40px; color: #666;">
        所有服务通过Docker容器部署，使用docker-compose管理。
    </p>
</body>
</html>';
        add_header Content-Type text/html;
    }
}